import { PickTimeValidatorDirective } from './pick-time-validator.directive';

describe('PickTimeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PickTimeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
