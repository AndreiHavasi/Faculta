import { LeaveTimeValidatorDirective } from './leave-time-validator.directive';

describe('LeaveTimeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LeaveTimeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
